<p id="notice"><%= notice %></p>
<h3>Listagem de arquivos</h3>

<table class="table">
  <thead class="thead-dark">
    <tr>
      <th Arquivo></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @managers.each do |manager| %>
      <tr>
        <td><%= link_to "arquivo", manager.file_txt.url %></td>
        <td><%= link_to 'Show', manager ,class: "btn btn-success btn-sm"%></td>
        <td><%= link_to 'Edit', edit_manager_path(manager) ,class: "btn btn-warning btn-sm"%></td>
        <td><%= link_to 'Destroy', manager,class: "btn btn-danger btn-sm" ,method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo arquivo', new_manager_path ,class: "btn btn-primary btn-sm"%>
